[pytest]
DJANGO_SETTINGS_MODULE = backend.settings.dev
python_files = tests.py test_*.py *_tests.py
addopts = -ra -v --reuse-db --nomigrations
markers =
    unit: Unit tests for services and pure logic.
    integration: Integration tests for API endpoints.
    permissions: Tests specifically for permission logic.
    feed: Tests for the feed functionality.
